#cloud-config
package_update: true
packages:
  - nginx
runcmd:
  - systemctl enable nginx
  - systemctl start nginx
  - echo "<h1>Welcome to Azure VM managed by Terraform & GitHub Actions 🚀</h1>" > /var/www/html/index.html
