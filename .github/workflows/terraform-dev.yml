name: 🚀 Terraform Azure VM with Nginx

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      TF_WORKING_DIR: ./environments/dev
      LOCATION: centralus
      AZURE_RESOURCE_GROUP: rg-dev-vm
      VM_NAME: dev-vm-instance
      VM_SIZES: Standard_B1s Standard_B2s Standard_B2ms

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🧰 Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      # ✅ Auto-format Terraform files
      - name: 📝 Terraform Format
        run: terraform -chdir=${{ env.TF_WORKING_DIR }} fmt -recursive

      # ✅ Extract Azure credentials from JSON secret
      - name: 🔐 Extract Azure Credentials
        id: azure-creds
        run: |
          echo "CLIENT_ID=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.clientId')" >> $GITHUB_OUTPUT
          echo "CLIENT_SECRET=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.clientSecret')" >> $GITHUB_OUTPUT
          echo "SUBSCRIPTION_ID=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.subscriptionId')" >> $GITHUB_OUTPUT
          echo "TENANT_ID=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.tenantId')" >> $GITHUB_OUTPUT

      # ✅ Create terraform.tfvars with all required variables
      - name: 📄 Create terraform.tfvars
        run: |
          cat > ${{ env.TF_WORKING_DIR }}/terraform.tfvars << EOF
          location = "${{ env.LOCATION }}"
          resource_group_name = "${{ env.AZURE_RESOURCE_GROUP }}"
          vm_name = "${{ env.VM_NAME }}"
          vm_size = "Standard_B1s"
          
          # Azure authentication variables
          subscription_id = "${{ steps.azure-creds.outputs.SUBSCRIPTION_ID }}"
          tenant_id = "${{ steps.azure-creds.outputs.TENANT_ID }}"
          client_id = "${{ steps.azure-creds.outputs.CLIENT_ID }}"
          client_secret = "${{ steps.azure-creds.outputs.CLIENT_SECRET }}"
          EOF
          
          echo "✅ Created terraform.tfvars with all required variables"

      # ✅ Initialize Terraform
      - name: ⚙️ Terraform Init
        run: terraform -chdir=${{ env.TF_WORKING_DIR }} init -upgrade -input=false

      # ✅ Validate configuration
      - name: 🧪 Terraform Validate
        run: terraform -chdir=${{ env.TF_WORKING_DIR }} validate -no-color

      # ✅ Terraform Plan with VM size fallback
      - name: 🧭 Terraform Plan
        id: plan
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          PLAN_SUCCESS=false
          
          for size in ${{ env.VM_SIZES }}; do
            echo "💻 Trying VM size: $size"
            
            # Update the vm_size in tfvars for this attempt
            sed -i "s/vm_size = \".*\"/vm_size = \"$size\"/" terraform.tfvars
            
            if terraform plan -no-color -out=tfplan -var-file="terraform.tfvars"; then
              echo "✅ Plan successful with VM size: $size"
              echo "SELECTED_VM_SIZE=$size" >> $GITHUB_ENV
              PLAN_SUCCESS=true
              break
            else
              echo "⚠️  Plan failed with VM size: $size, trying next..."
              rm -f tfplan
            fi
          done
          
          if [ "$PLAN_SUCCESS" = false ]; then
            echo "❌ No suitable VM size found from: ${{ env.VM_SIZES }}"
            exit 1
          fi

      # ✅ Apply Terraform changes
      - name: 🚀 Terraform Apply
        run: |
          echo "🚀 Applying Terraform plan with VM size: ${{ env.SELECTED_VM_SIZE }}"
          terraform -chdir=${{ env.TF_WORKING_DIR }} apply -auto-approve -input=false tfplan

      # ✅ Get Terraform outputs
      - name: 📤 Get Outputs
        id: outputs
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          echo "public_ip=$(terraform output -raw public_ip)" >> $GITHUB_OUTPUT
          echo "resource_group=$(terraform output -raw resource_group_name 2>/dev/null || echo '${{ env.AZURE_RESOURCE_GROUP }}')" >> $GITHUB_OUTPUT
          echo "vm_name=$(echo '${{ env.VM_NAME }}')" >> $GITHUB_OUTPUT

      # ✅ Health check
      - name: 🩺 Health Check
        run: |
          echo "🔍 Running health checks on VM at ${{ steps.outputs.public_ip }}..."
          MAX_RETRIES=12
          COUNTER=0
          
          until curl -f -s -o /dev/null -w "HTTP Status: %{http_code}\n" http://${{ steps.outputs.public_ip }}; do
            if [ $COUNTER -eq $MAX_RETRIES ]; then
              echo "❌ Health check failed after $MAX_RETRIES attempts"
              echo "VM might be running but Nginx not responding properly"
              exit 1
            fi
            echo "⏳ Waiting for Nginx to be ready... ($((COUNTER+1))/$MAX_RETRIES)"
            sleep 30
            COUNTER=$((COUNTER+1))
          done
          
          echo "✅ Health check passed! Nginx is responding."

      # ✅ Display deployment info
      - name: 🌐 Show Deployment Info
        run: |
          echo "🎉 Deployment Successful!"
          echo "========================"
          echo "🌍 Public IP: ${{ steps.outputs.public_ip }}"
          echo "🖥️  VM Name: ${{ steps.outputs.vm_name }}"
          echo "📦 Resource Group: ${{ steps.outputs.resource_group }}"
          echo "💻 VM Size: ${{ env.SELECTED_VM_SIZE }}"
          echo "⚡ SSH Command: ssh azureuser@${{ steps.outputs.public_ip }}"
          echo "🔗 Website URL: http://${{ steps.outputs.public_ip }}"
          echo "🔍 Test Command: curl http://${{ steps.outputs.public_ip }}"

      # ✅ Create deployment summary
      - name: 📋 Create Deployment Summary
        if: always()
        run: |
          echo "## 🚀 Terraform Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ job.status }}" == "success" ]; then
            echo "### ✅ Deployment Successful!" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Deployment Details:**" >> $GITHUB_STEP_SUMMARY
            echo "- 🌐 **Website URL**: [http://${{ steps.outputs.public_ip }}](http://${{ steps.outputs.public_ip }})" >> $GITHUB_STEP_SUMMARY
            echo "- 🔗 **Public IP**: ${{ steps.outputs.public_ip }}" >> $GITHUB_STEP_SUMMARY
            echo "- 🖥️ **VM Name**: ${{ steps.outputs.vm_name }}" >> $GITHUB_STEP_SUMMARY
            echo "- 📦 **Resource Group**: ${{ steps.outputs.resource_group }}" >> $GITHUB_STEP_SUMMARY
            echo "- 💻 **VM Size**: ${{ env.SELECTED_VM_SIZE }}" >> $GITHUB_STEP_SUMMARY
            echo "- ⚡ **SSH Command**: \`ssh azureuser@${{ steps.outputs.public_ip }}\`" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**What was deployed:**" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Ubuntu 22.04 LTS VM" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Nginx web server" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ SSH access with generated key" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Network Security Group with HTTP(80) and SSH(22) access" >> $GITHUB_STEP_SUMMARY
          else
            echo "### ❌ Deployment Failed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Check the workflow logs for detailed error information." >> $GITHUB_STEP_SUMMARY
          fi