name: 🚀 Terraform Azure VM with Nginx

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      TF_WORKING_DIR: ./environments/dev
      LOCATION: eastus  # Changed from centralus to avoid limits
      AZURE_RESOURCE_GROUP: rg-dev-vm-eastus
      VM_NAME: dev-vm-instance
      VM_SIZES: Standard_B1s Standard_B2s Standard_B2ms

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🧰 Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      # ✅ Use Service Principal authentication (compatible with your Terraform code)
      - name: 🔐 Azure Login (Service Principal)
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: 🔍 Verify Azure Login
        run: |
          echo "✅ Logged into Azure subscription:"
          az account show --query name -o tsv

      # ✅ Create terraform.tfvars with all required variables
      - name: 📄 Create terraform.tfvars
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          cat > terraform.tfvars << 'EOF'
location = "${{ env.LOCATION }}"
resource_group_name = "${{ env.AZURE_RESOURCE_GROUP }}"
vm_name = "${{ env.VM_NAME }}"
vm_size = "Standard_B1s"
EOF
          echo "✅ Created terraform.tfvars"

      # ✅ Initialize Terraform
      - name: ⚙️ Terraform Init
        run: terraform -chdir=${{ env.TF_WORKING_DIR }} init -upgrade -input=false

      # ✅ Validate configuration
      - name: 🧪 Terraform Validate
        run: terraform -chdir=${{ env.TF_WORKING_DIR }} validate -no-color

      # ✅ Terraform Plan with VM size fallback
      - name: 🧭 Terraform Plan
        id: plan
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          PLAN_SUCCESS=false
          
          for size in ${{ env.VM_SIZES }}; do
            echo "💻 Trying VM size: $size"
            
            # Update vm_size in tfvars
            sed -i "s/vm_size = \".*\"/vm_size = \"$size\"/" terraform.tfvars
            
            if terraform plan -no-color -out=tfplan -var-file="terraform.tfvars"; then
              echo "✅ Plan successful with VM size: $size"
              echo "SELECTED_VM_SIZE=$size" >> $GITHUB_ENV
              PLAN_SUCCESS=true
              break
            else
              echo "⚠️ Plan failed with VM size: $size, trying next..."
              rm -f tfplan
            fi
          done
          
          if [ "$PLAN_SUCCESS" = false ]; then
            echo "❌ No suitable VM size found from: ${{ env.VM_SIZES }}"
            exit 1
          fi

      # ✅ Apply Terraform changes
      - name: 🚀 Terraform Apply
        run: |
          echo "🚀 Applying Terraform plan with VM size: ${{ env.SELECTED_VM_SIZE }}"
          terraform -chdir=${{ env.TF_WORKING_DIR }} apply -auto-approve -input=false tfplan

      # ✅ Get Terraform outputs
      - name: 📤 Get Outputs
        id: outputs
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          echo "public_ip=$(terraform output -raw public_ip)" >> $GITHUB_OUTPUT
          echo "website_url=$(terraform output -raw website_url)" >> $GITHUB_OUTPUT

      # ✅ Health check
      - name: 🩺 Health Check
        run: |
          echo "🔍 Running health checks on VM at ${{ steps.outputs.public_ip }}..."
          MAX_RETRIES=10
          COUNTER=0
          
          until curl -f -s http://${{ steps.outputs.public_ip }}; do
            if [ $COUNTER -eq $MAX_RETRIES ]; then
              echo "❌ Health check failed after $MAX_RETRIES attempts"
              exit 1
            fi
            echo "⏳ Waiting for VM to be ready... ($((COUNTER+1))/$MAX_RETRIES)"
            sleep 30
            COUNTER=$((COUNTER+1))
          done
          
          echo "✅ Health check passed! Nginx is responding."

      # ✅ Display deployment info
      - name: 🌐 Show Deployment Info
        run: |
          echo "🎉 Deployment Successful!"
          echo "========================"
          echo "🌍 Public IP: ${{ steps.outputs.public_ip }}"
          echo "🔗 Website URL: ${{ steps.outputs.website_url }}"
          echo "💻 VM Size: ${{ env.SELECTED_VM_SIZE }}"
          echo "⚡ SSH Command: ssh azureuser@${{ steps.outputs.public_ip }}"