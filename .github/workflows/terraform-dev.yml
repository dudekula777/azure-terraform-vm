name: Deploy Azure VM with Nginx via Terraform

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      TF_WORKING_DIR: ./environments/dev
      AZURE_RESOURCE_GROUP: rg-dev-vm
      LOCATION: centralus
      VM_NAME: dev-vm
      VM_SIZES: "Standard_B1s Standard_B2s Standard_B2ms"

    steps:
      # ✅ 1. Checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # ✅ 2. Azure Login
      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.ARM_CLIENT_ID }}
          tenant-id: ${{ secrets.ARM_TENANT_ID }}
          subscription-id: ${{ secrets.ARM_SUBSCRIPTION_ID }}

      # ✅ 3. Setup Terraform
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.5

      # ✅ 4. Terraform Init & Validate
      - name: Initialize Terraform
        run: terraform -chdir=$TF_WORKING_DIR init -input=false

      - name: Validate Terraform configuration
        run: terraform -chdir=$TF_WORKING_DIR validate -no-color

      # ✅ 5. Ensure RG Exists
      - name: Ensure Resource Group Exists
        run: |
          if [ "$(az group exists --name $AZURE_RESOURCE_GROUP)" = "false" ]; then
            echo "🆕 Creating resource group..."
            az group create --name $AZURE_RESOURCE_GROUP --location $LOCATION
          else
            echo "✅ Resource group already exists."
          fi

      # ✅ 6. Terraform Plan with fallback for VM sizes
      - name: Terraform Plan with VM Size Fallback
        id: plan
        run: |
          set -e
          PLAN_SUCCESS=false
          for size in $VM_SIZES; do
            echo "💻 Trying VM size: $size"
            terraform -chdir=$TF_WORKING_DIR plan -no-color -out=tfplan \
              -var "subscription_id=${{ secrets.ARM_SUBSCRIPTION_ID }}" \
              -var "tenant_id=${{ secrets.ARM_TENANT_ID }}" \
              -var "client_id=${{ secrets.ARM_CLIENT_ID }}" \
              -var "client_secret=${{ secrets.ARM_CLIENT_SECRET }}" \
              -var "location=$LOCATION" \
              -var "vm_size=$size" \
              && PLAN_SUCCESS=true && break
          done
          if [ "$PLAN_SUCCESS" = false ]; then
            echo "❌ No suitable VM size found."
            exit 1
          fi

      # ✅ 7. Terraform Apply (Safe RG Import)
      - name: Terraform Apply
        if: github.ref == 'refs/heads/main'
        run: |
          echo "🚀 Applying Terraform plan..."
          if terraform -chdir=$TF_WORKING_DIR state list | grep -q "azurerm_resource_group.main"; then
            echo "✔️ RG already tracked by Terraform."
          else
            echo "📦 Importing existing RG..."
            terraform -chdir=$TF_WORKING_DIR import \
              azurerm_resource_group.main \
              "/subscriptions/${{ secrets.ARM_SUBSCRIPTION_ID }}/resourceGroups/${AZURE_RESOURCE_GROUP}" || true
          fi

          terraform -chdir=$TF_WORKING_DIR apply -auto-approve tfplan

      # ✅ 8. Show Outputs
      - name: Show Outputs
        run: terraform -chdir=$TF_WORKING_DIR output -no-color
